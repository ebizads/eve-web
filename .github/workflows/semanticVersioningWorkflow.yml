name: Semantic Versioning Workflow dev/test

on:
  push:
    branches:
      - dev/test
      - master
  workflow_dispatch:

jobs:
  release: 
    permissions:
      contents: write
      issues: write
      pull-requests: write
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.14.0
      - run: npm ci
      - run: npm run build
      - run: npx semantic-release
        env: 
          GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
